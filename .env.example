# ISA Environment Configuration (Template)
#
# Copy to `.env` and fill values. Never commit `.env`.
#
# Notes:
# - `JWT_SECRET` must be >= 32 characters (server validation).
# - TiDB Cloud requires TLS. Use `DATABASE_URL` with an SSL query param:
#   - `?sslmode=require` or `?ssl=true` or `?ssl-mode=required`
#   (Parsed by `server/db-connection.ts`.)
# - If you store multi-line secrets in `.env` (e.g. GitHub App private key),
#   keep it single-line with `\\n` escapes and quote it:
#   `GITHUB_APP_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----"`
#
# Verified quick checks:
# - `pnpm run env:check`
# - `node scripts/check-env.js`
# - `pnpm exec tsx scripts/check-db-status.ts`
#

# Core (required for server start)
VITE_APP_ID=
JWT_SECRET=
DATABASE_URL=

# Auth / Manus
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
MANUS_API_KEY=
VITE_FRONTEND_FORGE_API_URL=

# LLM
OPENAI_API_KEY=
OPENAI_API_BASE=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_KEY=

# GitHub (user tokens)
GITHUB_PAT=
GH_TOKEN=

# GitHub App (optional unless you use App flows)
GITHUB_APP_PRIVATE_KEY=
GITHUB_APP_ID=
INSTALLATION_ID=

# TiDB (optional; keep if your deployment needs keypair auth)
TIDB_PRIVATE_KEY=
TIDB_PUBLIC_KEY=

# Cron
CRON_SECRET=

# Email (optional)
SENDGRID_API_KEY=
SENDER_EMAIL=
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASSWORD=

# Runtime toggles (optional)
NODE_ENV=
PORT=
VITE_APP_URL=
ISA_ADVISORY_VERSION=
RUN_DB_TESTS=
RUN_CELLAR_TESTS=
VITEST=
AppID=

# Alternate DB vars (optional; used by some scripts)
DB_HOST=
DB_NAME=
DB_PASSWORD=
DB_USER=

# Frontend analytics (optional)
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# Portal URL (optional)
VITE_OAUTH_PORTAL_URL=

